<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <description>dubbo服务消费者配置</description>
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="XXX" owner="XXX" organization="dubbox1"/>
    <!--向 zookeeper 订阅 provider 的地址，由 zookeeper 定时推送-->
    <dubbo:registry address="zookeeper://115.47.154.107:2181"/>
    <!--使用 dubbo 协议调用定义好的 api.PermissionService 接口-->
    <!-- 生成远程服务代理，可以和本地bean一样使用demoService -->
    <dubbo:reference id="testService" interface="com.ruifight.oa.service.TestService" check="false"/>

    <!-- 服务者与消费者的默认配置 -->
    <!-- 延迟到Spring初始化完成后，再暴露服务,服务调用超时设置为6秒,超时不重试   -->
    <!--<dubbo:provider delay="-1" timeout="6000" retries="0"/>-->
    <dubbo:consumer timeout="6000" retries="0"/>
</beans>