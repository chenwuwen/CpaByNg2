<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <description>dubbo服务提供者配置</description>

    <!--定义了提供方应用信息，用于计算依赖关系；在 dubbo-admin 或 dubbo-monitor 会显示这个名字，方便辨识,其值常设置为项目名-->
    <dubbo:application name="demotest-provider" owner="programmer" organization="dubbox"/>

    <!-- 使用multicast广播注册中心暴露服务地址 -->
    <!--<dubbo:registry address="multicast://224.5.6.7:1234" /> -->
    <!--使用 zookeeper 注册中心暴露服务，注意要先开启 zookeeper-->
    <dubbo:registry address="zookeeper://115.47.154.107:2181"/>

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20880"/>
    <!-- 是否纳入调用统计报表（可选）计数 -->
    <dubbo:monitor protocol="registry"/>

    <!--使用 dubbo 协议实现定义好的 api.PermissionService 接口-->
    <!-- 声明需要暴露的服务接口 -->
    <!--这里我讲试题库的接口提供出去-->
    <dubbo:service interface="cn.kanyun.cpa.service.itempool.ICpaRepertoryService" ref="cpaRepertoryService" protocol="dubbo"/>
    <!--具体实现该接口的 bean-->
    <!--试题库的接口的实现类-->
    <bean id="cpaRepertoryService" class="cn.kanyun.cpa.service.itempool.impl.CpaRepertoryServiceImpl"/>

    <!-- 服务者与消费者的默认配置 -->
    <!-- 延迟到Spring初始化完成后，再暴露服务,服务调用超时设置为6秒,超时不重试   -->
    <dubbo:provider delay="-1" timeout="6000" retries="0"/>
    <!--<dubbo:consumer timeout="6000" retries="0"/>-->
</beans>